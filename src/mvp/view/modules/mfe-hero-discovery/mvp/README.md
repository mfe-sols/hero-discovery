# Hero Discovery MVP

Role-based entry points for the `hero-discovery` module. Root files remain compatibility exports; prefer this folder when expanding the module with API services or app-specific presenters.
